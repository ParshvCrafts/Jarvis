# =============================================================================
# JARVIS Core Dependencies
# Minimum required to run in TEXT mode
# Install: pip install -r requirements-core.txt
# =============================================================================

# Configuration & Environment
python-dotenv>=1.0.0
pyyaml>=6.0.1
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Logging
loguru>=0.7.2

# HTTP & Async
httpx>=0.25.0
requests>=2.31.0
aiofiles>=23.2.1
aiohttp>=3.9.0
nest-asyncio>=1.5.8

# LLM Providers (at least one needed)
groq>=0.11.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-groq>=0.2.0

# Memory (basic)
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# Keyboard shortcuts (optional, for hotkey activation)
keyboard>=0.13.5

# Finance (stock data)
yfinance>=0.2.36
